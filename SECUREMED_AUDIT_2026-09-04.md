# SecureMed — تقرير استكشاف ومراجعة تقنية

**التاريخ:** 2026-09-04 · **النطاق:** مراجعة قراءة فقط، لم يُعدَّل أي ملف من المشروع.

كل بند موسوم بـ ✅ **تحققتُ منه بقراءة الملف مباشرة**. البنود الموسومة بـ ⚠️ وردت من فحص آلي ولم أفتح ملفها سطراً بسطر، فتحقَّق منها قبل التصرف.

---

## 1. نبذة

SecureMed منصة إدارة سجلات صحية موجَّهة للسوق العربي (واجهة RTL كاملة، وثائق بالعربية). ليست نموذجاً أولياً: هي نظام واسع فعلاً — 19 تطبيق Django، 45 موديل، ~19.6 ألف سطر Python، و28 صفحة React بـ~17 ألف سطر TypeScript، بالإضافة إلى تطبيق أندرويد أصلي بـ57 ملف Kotlin، ومنظومة DevSecOps، وتوثيق نشر.

النموذج التنظيمي المميز فيه هو **الأحواض الصحية** (`apps/basins`) كوحدة عزل بين المستشفيات/المناطق، و**قنوات التواصل الطبي** (`apps/channels`) التي تجمع فريق العلاج حول مريض واحد. فوق ذلك 11 دوراً وظيفياً، وتغطية للمسارات السريرية الكاملة: مواعيد، صيدلية، مختبر، أقسام تنويم، فواتير، طب اتصالي (WebRTC)، تقارير PDF/Excel عربية، تحليلات، تكامل HL7/FHIR، ومساعد ذكاء اصطناعي.

الأمن هو واجهة بيع المشروع: تشفير حقول بـ Fernet، JWT بتدوير وقائمة حظر، TOTP وWebAuthn، بصمة أجهزة، WAF داخلي، سلسلة تدقيق بالهاش، ومسح ثغرات.

**الحكم العام:** الطموح المعماري والاتساع الوظيفي مثيران للإعجاب، وكثير من الأنماط الصحيحة موجود بالفعل في الشيفرة. لكن **الفجوة بين ما تعلنه الوثائق وما تنفّذه الشيفرة واسعة**، والمشكلة الأخطر أن هذه الفجوة تقع في المسارات الأمنية تحديداً — أي في المكان الذي لا يحتمل فيه نظام طبي فرقاً بين الادعاء والتنفيذ. هذا هو النمط الأهم في التقرير كله، وأغلب المقترحات أدناه تعود إليه.

---

## 2. الهيكل

| الطبقة | التقنية | الحجم |
|---|---|---|
| Backend | Django 5.0 · DRF · SimpleJWT · Celery · Channels/daphne | 19 تطبيق · 45 موديل · ~19.6k سطر |
| Frontend | React 18 · TypeScript · Vite · Tailwind · Zustand · React Query | 59 ملف · 28 صفحة · ~17k سطر |
| Android | Kotlin · Jetpack Compose · Room+SQLCipher · Hilt | 57 ملف |
| ai-service | Express · z-ai-web-dev-sdk (GLM) · منفذ 8100 | 288 سطر (معظمه معطّل) |
| البيانات | PostgreSQL (إنتاج) · SQLite (تطوير) | كل المفاتيح UUID · صفر ManyToMany |

الأوامر موحّدة في `Makefile` بالجذر (`make dev` / `test` / `security-scan` / `docker-up`) وهي نقطة قوة — التوثيق التشغيلي أفضل من المتوسط.

---

## 3. الفجوات الأمنية — مرتَّبة بالخطورة

### 3.1 بحث عام يتجاوز كل ضوابط الوصول ✅

`backend/apps/accounts/views.py:838-866` — `GlobalSearchView`. التوصيف يقول حرفياً *"Results are permission-scoped"*، لكن حلقة المرضى:

```python
for p in Patient.objects.all()[:500]:
```

بلا أي تقييد بدور أو حوض. أي مستخدم مُصدَّق — **بما في ذلك دور `PATIENT` نفسه** — يستطيع البحث والحصول على `full_name` و`national_id`، وكلاهما حقل مشفَّر في قاعدة البيانات يُفَك تشفيره ويُعاد في الاستجابة. هذه أخطر ثغرة تفويض في المشروع: تُفرِّغ نموذج الأحواض والقنوات من مضمونه بنداء واحد. (وهي أيضاً مشكلة أداء: فكّ تشفير 500 سجل عند كل ضغطة مفتاح.)

### 3.2 الكاش المحلي يُبطل كل الضوابط المعتمدة عليه ✅

`backend/config/settings.py:570-575`:

```python
CACHES ={'default':{'BACKEND':'django.core.cache.backends.locmem.LocMemCache', ...}}
```

هذا الكتلة موضوعة قرب **نهاية** الملف فتتجاوز أي تعريف سابق. والضوابط التالية كلها مبنية على الكاش: تحديد معدل الطلبات، حظر الـIP والأجهزة، عدّاد محاولات الدخول الفاشلة، حالة `mfa_pending`، وقائمة حظر JWT. مع `LocMemCache` تصبح الذاكرة **منفصلة لكل عملية gunicorn** — أي أن حدّ «5 محاولات دخول» يصبح فعلياً 5 × عدد العمال، والجهاز المحظور في عامل يبقى مسموحاً في الآخر.

وهذا **انحراف عن التصميم الأصلي لا اختيار مقصود**: التعليق الأصلي المحفوظ في `comments_mapping.json` تحت `Comment_547` نصّه `# Cache (Redis for sessions, rate-limiting, Celery-adjacent caching)`، و`Comment_548` نصّه `# Degrade gracefully if Redis is down` — أي أن المقصود كان Redis مع تدهور آمن. كما أن `DEVELOPMENT_LOG.md` يسجّل إصلاحاً سابقاً بـ`FileBasedCache` تراجع.

### 3.3 ملفات المرضى تُخدَم بلا مصادقة ✅

`backend/config/urls.py:32-35` يعرّف `_serve_media`، و`94-97` يسجّله في الإنتاج:

```python
re_path(r'^media/(?P<path>.*)$', _serve_media, name='media-prod')
```

`django.views.static.serve` بلا أي تحقق هوية. كل مرفق طبي (تقارير، صور DICOM، وثائق) متاح لمن يعرف الرابط، ويتجاوز فحوص القنوات والأحواض بالكامل. الحماية الوحيدة هي غموض أسماء UUID — وهي ليست حماية، لأن الروابط تُسجَّل في السجلات وترسل في الإشعارات وتظهر في `Referer`.

يزيد الأمر أن **الملفات نفسها غير مشفَّرة على القرص**: `MedicalFile.file` حقل `FileField` عادي (⚠️ `apps/patients/models.py:290-295`)، بينما `encrypt_with_aes` موجودة في `apps/security/crypto.py` وغير مستخدمة.

### 3.4 تحديث الرمز يُلغي إعدادات JWT الصارمة ✅

`backend/apps/accounts/views.py:280-300` — `RefreshTokenView` بـ`AllowAny`:

```python
token = RefreshToken(refresh_token)
return Response({'access': str(token.access_token)})
```

لا تدوير، لا إضافة للقائمة السوداء، لا إعادة ربط بالبصمة. الإعدادات في `settings.py:380-407` تضبط `ROTATE_REFRESH_TOKENS=True` و`BLACKLIST_AFTER_ROTATION=True`، لكن هذا العرض المخصص لا يستخدم `TokenRefreshView` من SimpleJWT فلا يمرّ بذلك المنطق أبداً. النتيجة: رمز تحديث مسروق صالح ليوم كامل ويُنتج رموز وصول بلا حد.

### 3.5 المصادقة الحيوية عرض توضيحي لا آلية أمنية ⚠️

الخلل موجود على الطرفين:

- **الواجهة** (`frontend/src/utils/webauthn.ts`, 428 سطراً): تولّد الـchallenge **في المتصفح** عبر `crypto.getRandomValues` — والتعليق نفسه يقول "in production, this comes from the server". و`Login.tsx:136-137` يرسل `result.assertion?.id` فقط كنص، بلا توقيع. و`credentialId` مخزَّن في `localStorage`.
- **الخادم**: `BiometricLoginSerializer` يستقبل `biometric_template` كنص من العميل ويعيد هَشّه ويقارن — أي أنه **سرّ مشترك قابل للإعادة**، لا تحقق تشفيري. و`generate_challenge()` في `apps/security/crypto.py` تعيد `hmac:session_key` و`verify_challenge()` تنجح إذا أعاد العميل نفس القيمة. حقلا `BiometricProfile.public_key` و`private_key_encrypted` غير مستخدمين.

بالإضافة إلى خطأ تشغيلي: الدخول يستعلم `BiometricProfile.objects.get(user=user, is_active=True)` متجاهلاً `device_id`، فيرمي `MultipleObjectsReturned` (خطأ 500) لأي مستخدم بجهازين.

### 3.6 خوارزمية JWT غير التي يعلنها النظام ⚠️

`settings.py` تختار RS256 فقط إذا وُجد `backend/certs/jwt_private.pem` و`jwt_public.pem`. المجلد **خالٍ من ملفات pem**، فالنظام يعمل HS256 على `SECRET_KEY` بصمت. وفي المقابل `SecurityDashboardView` (`apps/security/views.py:115-125`) يعيد قائمة **ثابتة مكتوبة يدوياً** تتضمن `jwt_algorithm: 'RS256'` و`encryption_at_rest: True` و`waf_active: True` بغض النظر عن الإعداد الفعلي. لوحة الأمن تُطمئن كذباً — وهذا أسوأ من عدم وجودها.

### 3.7 سلسلة التدقيق قابلة للتلاعب، ووسيط التدقيق ميت ⚠️

- الهاش في `AuditLog` هو **SHA-256 عادي لا HMAC** → من يملك وصولاً لقاعدة البيانات يستطيع تعديل سجل وإعادة بناء السلسلة كاملة.
- `AuditLogAdmin` يضبط `readonly_fields` لكنه **لا يعطّل `has_delete_permission`**، و`WAFMiddleware` يستثني `/admin/` أصلاً.
- `AuditLogMiddleware` و`SessionSecurityMiddleware` **معطّلان فعلياً** لطلبات API: `request.user` هو `AnonymousUser` في طبقة الوسطاء لأن DRF يصادق لاحقاً.
- وفي `apps/audit/middleware.py` خطأ إزاحة يضع `log_security_event` (الأسطر 44-58) **داخل** شرط `if 'settings' in request.path or 'config' in request.path` (السطر 38).
- `apps/audit/utils.py` يثق بترويسة `HTTP_X_FORWARDED_FOR` بلا شرط، بينما `get_client_ip` في accounts تشترط `TRUST_X_FORWARDED_FOR` → عناوين IP في سجل التدقيق قابلة للانتحال.

### 3.8 ضوابط أخرى بثقوب معروفة ⚠️

`RateLimitMiddleware` يستخدم `cache.get` + `cache.set` بلا `incr` ذرّي → قابل للتجاوز بالتزامن. `BoundJWTAuthentication` يربط الجلسة بـ`sha256(IP:User-Agent)` لكنه **يتخطى الفحص كاملاً عند غياب المطالبة**، والمطالبة تُضاف لرموز التحديث فقط. رمز OTP بالبريد يُولَّد بـ`random.randint` لا `secrets` ويُقارن بـ`==` لا `compare_digest` (`apps/accounts/views.py:191-193`). CSP يحتوي `'unsafe-inline'`. `SanitizedJSONParser` يمرّر **كل** النصوص الواردة على `bleach.clean` → يشوّه بصمت كلمات المرور التي تحتوي `&` أو `<` ويعدّل الملاحظات الطبية الحرة (خطر على سلامة البيانات لا على الأمن فقط). رفع الملفات يتحقق من الامتداد وحجم 20MB فقط: **لا فحص magic bytes ولا مكافحة فيروسات**، و`original_filename` يُعاد في `Content-Disposition`.

### 3.9 تسريب سرّ الـTOTP لطرف ثالث ✅

`frontend/src/pages/SecuritySettings.tsx:264`:

```tsx
src={`https://api.qrserver.com/v1/create-qr-code/?...&data=${encodeURIComponent(mfaSetupData.qr_uri)}`}
```

`qr_uri` هو رابط `otpauth://` الذي **يحتوي السرّ المشترك للتحقق الثنائي**، ويُرسل نصاً ظاهراً في سلسلة استعلام لخدمة خارجية. المفارقة أن `Profile.tsx:331` يفعلها صحيحاً عبر `qr_image` المولَّد في الخادم — فالإصلاح مجرد توحيد للمسارين.

### 3.10 تثبيت الشهادات في أندرويد وهمي ⚠️

`android/.../di/NetworkModule.kt:57-59` يثبّت هاشاً نائباً `sha256/AAAA...=` وللمضيف `api.securemed.local` فقط. في إصدار الإنتاج الحماية من MITM معطّلة فعلياً. وثيقة `docs/ANDROID_DEVELOPMENT_ROADMAP.md` تعترف بهذا — وهي أحدث الوثائق وأصدقها.

### 3.11 نقاط مكشوفة أخرى ✅

`config/urls.py:63` يسجّل `django_prometheus.urls` على الجذر → `/metrics` بلا مصادقة. والأسطر 86-88 تجعل `/api/schema` و`/api/docs` و`/api/redoc` عامة — أي خريطة كاملة للـAPI متاحة لغير المصدَّقين.

---

## 4. مشاكل التشغيل والنشر

### 4.1 تعارض بنيوي في `render.yaml` ✅⚠️

الخدمة معرّفة بـ`env: docker`، ومعنى ذلك أن Render يستخدم `Dockerfile` بالجذر، وأمره الأخير `CMD gunicorn config.wsgi:application`. النتيجة أثران خطيران:

1. **لا WebSockets في الإنتاج** — تشغيل WSGI يُسقط Channels بالكامل، فالإشعارات الفورية والطب الاتصالي (`ws/notifications/`، `ws/video_call/<room>/`) لا تعمل، مع أن `asgi.py` و`daphne` مُعدّان بشكل صحيح.
2. **الترحيلات لا تُنفَّذ إطلاقاً** — منطق `migrate` و`seed` موجود في `deploy/start.sh`، وهذا الملف **لا يُستدعى أبداً** في هذا المسار.

وإضافةً لذلك `render.yaml` **لا يعرّف خدمة لـ`ai-service`**، بينما `apps/patients/views.py:193-200` ما زال ينادي `POST {AI_SERVICE_URL}/case-summary` بمهلة 75 ثانية على `http://127.0.0.1:8100` الافتراضي → استجابة 503 مع انتظار طويل في النشر الحالي.

### 4.2 طبقة القنوات في الذاكرة ⚠️

`settings.py:144-148` تضبط `CHANNEL_LAYERS` على `InMemoryChannelLayer` رغم أن `channels-redis` مثبَّت في `requirements.txt:57`. نفس مشكلة الكاش: البثّ لا يعبر بين عمليات الخدمة.

### 4.3 `docker-compose.yml` بالجذر لا يقوم ✅⚠️

هو المكدّس الإنتاجي الحقيقي (postgres، pgbouncer، redis بكلمة مرور، backend بـdaphne، celery worker وbeat، nginx)، لكن السطرين 246-247 يربطان `./backend/certs/server.crt` و`server.key` وهما **غير موجودين** → nginx لا يبدأ في وضع `full`. كما أن `devsecops/docker/nginx.conf` مكتوب كوكيل عكسي يتطلب `/etc/nginx/ssl/server.crt`، بينما `Dockerfile.frontend` ينسخ `dist` ولا يخدمها فعلياً.

### 4.4 ثلاث نسخ من الـbackend ونظام تعتيم ✅

هذه أكبر مفاجأة في الاستكشاف، ولم يلتقطها أي فحص آلي:

1. `backend/` — النسخة العاملة، و**كل تعليقاتها (~570) مستبدلة بـ`# Comment_NNN`**. الأصل محفوظ في `comments_mapping.json` بالجذر، والسكربت `extract_comments.py` هو من فعل ذلك. **وكلا الملفين مستثنى في `.gitignore:31-32`** — أي أن مفتاح استعادة كل توثيق الشيفرة موجود على قرص واحد فقط وغير محفوظ في git. فقدانه = ضياع دائم لتوثيق المشروع.
2. `backend_obfuscated/` — نسخة معتَّمة بـ**Pyarmor 9.2.7 (trial)** بتاريخ 2026-09-03 بوسم `non-profits`. ترخيص تجريبي لا يصلح للإنتاج.
3. `SecureMed-main/` — نسخة أقدم وأصغر (163 ملف py مقابل 186، بلا android/docs/devsecops/.git) **وتعليقاتها الأصلية سليمة**، مفكوكة من `SecureMed-main.zip`، مستثناة في `.gitignore:60`، ولها `.env` و`db.sqlite3` خاصان.

هذه النسخ الثلاث هي السبب المباشر لعدة نتائج خاطئة في الفحص الآلي (انظر القسم 7) لأن الأدوات كانت تخلط بين ملفات النسخ.

---

## 5. الاختبارات وسلسلة DevSecOps

**رقم «196/196 ناجحة و88% تغطية» في `docs/TEST_RESULTS.md` غير قابل لإعادة الإنتاج من الشيفرة الحالية** ✅. السبب: لا يوجد **أي `conftest.py`** في المشروع (بحثتُ في `backend/` كله فلم أجد إلا نسخاً داخل البيئات الافتراضية)، بينما `tests/test_appointments.py` يعتمد على تجهيزات `admin_client` و`doctor_user` و`patient_obj` و`future_time` و`scheduled_appt` → فشل `fixture not found`. الملفات 15 والدوال ~262.

ما هو غير مغطّى بالاختبارات أخطر من العدد: WAF، سلسلة هاش التدقيق، رفع وتنزيل الملفات الطبية، عزل الأحواض، `RefreshTokenView`، `GlobalSearchView`، وكشف `/media/`.

مشاكل إضافية:

- `.bandit.yml` **مشوَّه** — من السطر 59 يستخدم `|` بدلاً من `#` للتعليقات (مثل `- B506  | yaml_load`) فتنتج معرّفات اختبار غير صالحة يرفضها bandit.
- `devsecops/github-actions/devsecops-pipeline.yml` **لا يُنفَّذ أبداً** لأنه خارج `.github/`، ومع ذلك شارة في `README` تشير إليه. المسار الحقيقي `.github/workflows/` فيه `tests.yml` (حقيقي وجيد: postgres 15 + redis 7) و`security-scan.yml` الذي **كل خطواته تنتهي بـ`|| true`** فلا يفشل البناء أبداً ولا يرفع SARIF، و`android.yml` بإجراءات مهجورة (`checkout@v3`, `upload-artifact@v3`).
- `devsecops/scripts/security-scan.sh` يتخطى أي أداة غير مثبَّتة بتحذير فقط → يمكن أن «ينجح» وهو لا يفحص شيئاً.
- `test_settings.py` يعطّل تحديد المعدل (السطر 54) ويستخدم `MD5PasswordHasher` (64-66) — مقبول للسرعة، لكنه يعني أن مسارات تحديد المعدل غير مختبَرة إطلاقاً.
- `frontend`: **صفر اختبارات**، و`"lint": "eslint src --ext ts,tsx"` معرَّف بينما **eslint غير مثبَّت** في التبعيات ولا يوجد ملف إعداد له. و`make test-frontend` و`lint-frontend` كلاهما مجرد `npm run build`.

---

## 6. جودة الشيفرة والصيانة

**تكرار وشيفرة ميتة:** التحقق الثنائي منفَّذ ثلاث مرات (`Profile.tsx`، `SecuritySettings.tsx`، `SettingsPage.tsx`)، وقوائم الأدوار مكرّرة بين `App.tsx` و`Layout.tsx:127-148`، و`VideoCallConsumer` معرَّف مرتين (`apps/appointments/consumers.py` و`apps/telemedicine/consumers.py`). ميت تماماً: `pages/Analytics.tsx` (309 سطراً، غير موصول بمسار)، `hooks/useRealtimeNotifications.ts` (82 سطراً، لا يُستورَد، ويقرأ `localStorage.getItem('access_token')` وهي دائماً null)، `apps/patients/file_serializers.py` (يستورد `apps.patients.file_models` غير الموجود)، و**4 من 5 نقاط في `ai-service`** بعد نقل الذكاء الاصطناعي إلى Gemini داخل Django.

**تعارضات صامتة في عقد الـAPI** ⚠️ — الواجهة تقرأ حقولاً لا يُصدرها Django: `res.data.structuredNote` مقابل `{"structured":...}`، و`triageResult.triageResult` مقابل `{level, reasoning, recommendations}`، و`err.response?.data?.error` مقابل `detail`. هذه أعطال لا تُظهر خطأً، فقط قيم فارغة.

**كتل إعدادات مكرّرة** ✅ — `UNFOLD` معرَّف مرتين (74-94 ثم 226-249، والثانية تُسقط `DASHBOARD_CALLBACK`)، و`USE_S3_STORAGE` معرَّف مرتين بدلالتين مختلفتين (`cast=bool` عند 274-284 و`== '1'` عند 597).

**الواجهة:** الرموز محفوظة في `sessionStorage` عبر Zustand persist فهي مقروءة بـJS ومعرَّضة لـXSS — مع أن `client.ts` يضبط `withCredentials: true` أي أن بنية الكوكيز نصف مبنية وغير مستخدمة. و`client.ts` يعيد حساب SHA-256 لبصمة الجهاز عند **كل** طلب بلا تخزين مؤقت. ومعالجة 403 تقارن **نصوصاً عربية** (`'تم حظر هذا الجهاز'`) فهي هشّة أمام أي تغيير في الصياغة. لا يوجد i18n — كل النصوص مضمّنة في JSX. والوضع الليلي يُطبَّق داخل `Layout.tsx:102-108` فقط، فصفحات `/login` و`/blocked` و`/forgot-password` تتجاهل الثيم المحفوظ. أكبر الملفات `PatientProfile.tsx` (1221 سطراً) و`Telemedicine.tsx` (795).

**إدارة المفاتيح:** التشفير Fernet + PBKDF2-HMAC-SHA256 بـ100 ألف تكرار من `ENCRYPTION_KEY` و`FIELD_ENCRYPTION_SALT` (`settings.py:412-417). لا KMS ولا HSM، **لا تدوير مفاتيح، ولا رقم إصدار مفتاح في النص المشفَّر**، وملح ثابت مشترك. هذا يعني أن تدوير مفتاح مستقبلاً سيتطلب إعادة تشفير كل الصفوف بلا مسار انتقالي.

**قيم افتراضية مضمَّنة:** `SECRET_KEY` (السطر 16)، `DB_PASSWORD` (206)، `ENCRYPTION_KEY` (414)، `INITIAL_ADMIN_PASSWORD` (537). حرس `ImproperlyConfigured` عند 618-630 يغطي **الأولين فقط** — أي أن نشراً بمفتاح تشفير افتراضي يمرّ بصمت. و`README.md:50-51` يحتوي بيانات دخول المدير الافتراضية.

**عبء القرص:** ~117 ألف ملف. المشروع فيه **بيئتان افتراضيتان متكرّرتان** (`backend/venv` ~7930 ملف py و`backend/.venv` ~7766)، بالإضافة إلى `node_modules` في مكانين، و`SecureMed-main/` بنسخها الخاصة، و`android/app/build/`، و`.gradle/`. كلها مستثناة من git لكنها تُبطئ كل أداة تعمل على المجلد.

---

## 7. تصحيحات — ادعاءات شائعة **خاطئة**

فحصتُ هذه بنفسي لأنها ظهرت كنتائج «حرجة» في الفحص الآلي، وكلها غير صحيحة. أوردها لأن العمل عليها إهدار وقت:

| الادعاء | الحقيقة |
|---|---|
| `config/asgi.py` فيه خطأ نحوي مميت | **سليم تماماً.** 26 سطراً، `AuthMiddlewareStack(URLRouter([...]))` صحيح والأقواس متوازنة. |
| `frontend/vite.config.ts` غير موجود وبيئة التطوير معطّلة | **موجود وسليم**، فيه `plugin-react` ووكيل لـ`/api` و`/ws` و`/ai` وضبط `base` للإنتاج. |
| `requirements.txt` فيه تثبيتات متعارضة تمنع التنصيب | **لا تعارض.** 69 سطراً، `boto3==1.34.110` و`django-storages==1.14.4` كل منهما مرة واحدة. |
| `*.txt` في `.gitignore:28` يكسر البناء | `requirements.txt` و`requirements-test.txt` **موجودان في فهرس git** (تحققت من `.git/index`) فالبناء يعمل. تبقى مخالفة كامنة: أي `.txt` جديد سيُفقد بصمت. |
| `apps.core` غير مسجَّل في `INSTALLED_APPS` فيتعطل | غير مسجَّل فعلاً، لكنه **لا يحتوي `models.py`** أصلاً (فقط `health.py`, `views.py`, `urls.py`, `mixins.py`) فلا ضرر. |
| `push_to_github.bat` مفقود | موجود، مع `push_to_github.sh`. |

الدرس: النسخ الثلاث من الـbackend تجعل أي فحص آلي غير مقيَّد بـ`backend/` يخلط الملفات. **قيّد أي بحث مستقبلي بـ`backend/`** واستثنِ `backend_obfuscated/` و`SecureMed-main/`.

---

## 8. خطة التطوير المقترحة

المبدأ الحاكم: **لا تُضِف ميزة واحدة قبل إغلاق المرحلة الأولى.** الاتساع الوظيفي ليس مشكلة المشروع؛ المشكلة أن الأساس الأمني المعلَن غير منفَّذ، وكل ميزة جديدة تُبنى على هذا الأساس تضاعف الدين.

### المرحلة صفر — قبل أي نشر آخر (أيام)

1. **قيّد `GlobalSearchView` بالدور والحوض.** استبدل `Patient.objects.all()` بـ`basin_scoped_queryset` مع منع صريح لدور `PATIENT`، وانقل الترشيح إلى قاعدة البيانات بدل حلقة Python. أخطر ثغرة في المشروع وأسرعها إصلاحاً.
2. **أعِد Redis إلى `CACHES`** مع التدهور الآمن الذي كان مقصوداً أصلاً (`Comment_547/548`)، **أو** — إن كانت البنية بعملية واحدة فعلاً — وثّق ذلك صراحةً وأضف حرساً يرفض الإقلاع بـ`LocMemCache` عندما `DEBUG=False`. الحالة الحالية أسوأ الاحتمالين: ضوابط تبدو موجودة ولا تعمل.
3. **أغلق `/media/`.** استبدل `_serve_media` بعرض يتحقق من الهوية وصلاحية المريض/القناة، أو انقل الملفات إلى S3 بروابط موقَّعة قصيرة الأجل (`django-storages` و`boto3` مثبَّتان بالفعل).
4. **أصلح تدوير رمز التحديث.** اجعل `RefreshTokenView` ترث `TokenRefreshView` من SimpleJWT أو نفّذ التدوير والحظر وإعادة ربط البصمة يدوياً.
5. **أوقف تسريب سرّ TOTP.** في `SecuritySettings.tsx:264` استخدم `qr_image` من الخادم كما يفعل `Profile.tsx` بالفعل.
6. **اجعل `SecurityDashboardView` يقرأ الإعداد الفعلي** بدل القيم الثابتة. لوحة أمنية تكذب أخطر من غيابها؛ ثم قرّر بوعي: إما تُصدر شهادات RS256 فعلاً، أو تعلن HS256 بصدق.

### المرحلة الأولى — إصلاح النشر والثقة بالاختبارات (أسبوعان)

7. **حُل تعارض ASGI/WSGI في `render.yaml`:** إما `dockerCommand` يشغّل `deploy/start.sh` (فتعمل الترحيلات وdaphne)، أو أوقف الوعد بالميزات الفورية. حالياً الطب الاتصالي والإشعارات الفورية لا تعمل في الإنتاج، والترحيلات لا تُنفَّذ.
8. **أضف `conftest.py`** بالتجهيزات التي تعتمد عليها الاختبارات، ثم شغّل الحزمة كاملة وحدّث `TEST_RESULTS.md` بالرقم الحقيقي. رقم غير قابل لإعادة الإنتاج أضرّ من عدم وجود رقم.
9. **حوّل `channels-redis`** إلى `CHANNEL_LAYERS` (مثبَّت أصلاً)، وأصلح ربط شهادات nginx في `docker-compose.yml:246-247`.
10. **أصلح سلسلة DevSecOps:** انقل `devsecops-pipeline.yml` إلى `.github/workflows/`، أزل `|| true` من `security-scan.yml` وارفع SARIF، أصلح تعليقات `.bandit.yml` من السطر 59، واجعل `security-scan.sh` يفشل عند غياب أداة بدل تخطّيها.
11. **اكتب اختبارات للثقوب المُصلَحة تحديداً** — عزل الأحواض، `/media/`، البحث العام، تدوير الرمز، سلسلة التدقيق. هذه الاختبارات هي ما يمنع الانحدار مستقبلاً، ومنها ما انحدر فعلاً (الكاش).

### المرحلة الثانية — بناء ما هو معلَن (شهر إلى شهرين)

12. **أعِد بناء المصادقة الحيوية بشكل حقيقي أو اسحب الوعد.** WebAuthn صحيح يعني: الـchallenge يُولَّد ويُخزَّن في الخادم، والخادم يتحقق من التوقيع بالمفتاح العام المخزَّن (الحقلان موجودان وغير مستخدمين). الحالة الحالية سرّ مشترك قابل للإعادة، وتسميته «بصمة» في واجهة طبية تُنشئ ثقة غير مستحقة. وأصلح استعلام `BiometricProfile` ليشمل `device_id`.
13. **قوِّ التدقيق:** HMAC بمفتاح منفصل بدل SHA-256 العادي، `has_delete_permission = False` في `AuditLogAdmin`، أصلح خطأ الإزاحة في `apps/audit/middleware.py:38-58`، ووحّد قراءة IP على `TRUST_X_FORWARDED_FOR`. وانقل التدقيق من الوسيط إلى طبقة DRF (`APIView.initial` أو إشارات) حيث يكون المستخدم معروفاً فعلاً.
14. **شفّر الملفات الطبية على القرص** واستخدم `encrypt_with_aes` الموجودة، وأضف فحص magic bytes ومكافحة فيروسات لمسار الرفع.
15. **أضف إصدار مفتاح إلى النص المشفَّر** (`v1:...`) الآن، قبل تراكم البيانات — بدونه لا يوجد مسار تدوير للمفاتيح إطلاقاً، وهذا مطلب صريح في HIPAA.
16. **انقل الرموز إلى كوكيز HttpOnly** — البنية نصف موجودة (`withCredentials: true`).
17. **حدّد مصير `ai-service`:** إما احذفه ووجّه `case-summary` إلى Gemini داخل Django، أو أضف له مصادقة وتحديد معدل وCORS وخدمة في `render.yaml`. اليوم هو نقطة نهاية بلا مصادقة تستمع على كل الواجهات، و4 من 5 نقاطها ميتة.

### المرحلة الثالثة — نظافة وقابلية صيانة (مستمر)

18. **احسم قضية التعليقات فوراً.** انسخ `comments_mapping.json` احتياطياً خارج المجلد الآن، ثم قرّر: إن كان الهدف حماية الملكية الفكرية فالمكان الصحيح هو الترخيص والعقود لا تشويه الشيفرة؛ وإن كان الهدف تسليم نسخة معتَّمة فاستعد التعليقات في `backend/` واحتفظ بالتعتيم في مخرج البناء وحده. الوضع الحالي — نسخة عمل بلا توثيق ومفتاح استعادة غير محفوظ في git — أعلى مخاطرة من كل ما سبق على المدى الطويل.
19. احذف `SecureMed-main/` (نسخة قديمة زائدة) وإحدى البيئتين الافتراضيتين، بعد نسخ `comments_mapping.json`.
20. أزل الشيفرة الميتة، وحّد التحقق الثنائي في مكوّن واحد، وحّد قوائم الأدوار في مصدر واحد، وأصلح تعارضات عقد الـAI، وأزل التعريفات المكرّرة في `settings.py`.
21. ثبّت eslint فعلاً أو أزل السكربت، أضف اختبارات واجهة (Vitest + React Testing Library)، وأدخل i18n قبل أن تتضاعف النصوص.
22. أصلح تثبيت الشهادة في `NetworkModule.kt:59` بهاش حقيقي وللمضيف الإنتاجي، أو أزل الوعد من الوثائق.
23. **وحّد الوثائق مع الواقع.** `README.md` يقول «مكتمل وجاهز للتشغيل» ويتناقض داخلياً في عدد الاختبارات (96 مقابل 196)، ويحتوي بيانات دخول افتراضية، وشاراته تشير إلى مستودع غير الحقيقي. وثيقة `ANDROID_DEVELOPMENT_ROADMAP.md` هي النموذج الصحيح: تذكر ما لا يعمل بصراحة.

---

## 9. الخلاصة

النقطة الواحدة التي تستحق الاحتفاظ بها: **المشروع مبني بمعرفة معمارية جيدة، لكن حالته الفعلية أضعف بكثير من حالته المعلَنة، والفرق يتركّز في الأمن.** ستة إصلاحات في المرحلة صفر — كلها صغيرة الحجم — تنقل النظام من «آمن على الورق» إلى «آمن فعلاً في المسارات الحرجة». وأهم تغيير غير تقني هو التوقف عن كتابة الوثائق بصيغة الإنجاز قبل التحقق، لأن لوحة أمن تعيد `RS256` وهي تعمل بـ`HS256` هي نفس المشكلة في شكل شيفرة.

