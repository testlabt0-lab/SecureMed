"""
Basins (الأحواز الصحية) app configuration.
"""
from django.apps import AppConfig


class BasinsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.basins'
    verbose_name = 'الأحواز الصحية'
